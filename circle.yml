machine:
  services:
    - neo4j
  node:
    version: 0.10.22
dependencies:
  override:
    - npm install -g mocha
    - npm install
test:
  override:
    - curl http://localhost:7474
    - NEO_HOST=http://localhost:7474 npm test