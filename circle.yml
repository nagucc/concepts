machine:
  services:
    - docker
  node:
    version: 0.10.22
dependencies:
  override:
    - docker run -itd -p 7474:7474 kbastani/docker-neo4j
    - npm install -g mocha
    - npm install
test:
  override:
    - sleep 50
    - NEO_HOST=http://localhost:7474 npm test