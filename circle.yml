machine:
  services:
    - neo4j
  node:
    version: 0.10.22
dependencies:
  override:
    - npm install -g mocha
    - npm install
test:
  override:
    - NEO_HOST=http://neo4j:neo4j@localhost:7474 npm test