machine:
  services:
    - docker
  node:
    version: 0.10.22
dependencies:
  override:
    - npm install -g mocha
    - npm install
    - docker run -itd -p 7474:7474 kbastani/docker-neo4j
test:
  override:
    - curl http://localhost:7474
    - NEO_HOST=http://localhost:7474 npm test